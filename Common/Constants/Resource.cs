using Playnite.SDK;using System;namespace ScreenSaver.Common.Constants{    public class Resource    {        public static string MAIN_MENU_START => _mAIN_MENU_START.Value;        private static readonly Lazy<string> _mAIN_MENU_START = new Lazy<string>(() => ToId("MAIN_MENU_START"));        public static string GAME_MENU_PREVIEW => _gAME_MENU_PREVIEW.Value;        private static readonly Lazy<string> _gAME_MENU_PREVIEW = new Lazy<string>(() => ToId("GAME_MENU_PREVIEW"));        private static string ToId(string id) => ResourceProvider.GetString(id);     } }